meta {
  name: Update Job
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/jobs/{{jobId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "status": "applied",
    "score": 85.5
  }
}
